/*
 * main.cpp
 * */

#include <gtest/gtest.h>
#include <string>
#include <fstream>
#include <sstream>

#include "field.h"

string ToString(const char* file) {
  
  while (!reading_file.eof()) {
    
  }
// first parameter is testcase name, second parameter is test name
TEST(FieldConstructur, integer) {
const string = "
  "W
  
  ASSERT_EQ(expected, actual);
}

TEST(Field
